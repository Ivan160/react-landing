import { resolveFromRoot } from 'config/utils';

export const SRC_PATH = resolveFromRoot('src');
export const ASSETS_PATH = resolveFromRoot('src/assets');
export const FONTS_PATH = resolveFromRoot('src/fonts');
export const ICONS_ELEMENTS_PATH = resolveFromRoot('src/assets/icons');
export const IMAGES_ELEMENTS_PATH = resolveFromRoot('src/assets/images');
export const MODULES_PATH = resolveFromRoot('src/modules');
export const CORE_PATH = resolveFromRoot('src/core');
export const SHARED_PATH = resolveFromRoot('src/shared');
export const API_PATH = resolveFromRoot('src/api');
export const BUILD_PATH = resolveFromRoot('build');
